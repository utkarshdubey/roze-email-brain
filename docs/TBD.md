# A small list of to be done (TBD) items for the agent to pick up on
### Shouldn't be touched by the agents directly, read-only.

- Add real gmail links instead of internal thread citations for better UX.
- Explore multi-threading/parallelization for non-dependent processes for all Gmail based data ingestion.
